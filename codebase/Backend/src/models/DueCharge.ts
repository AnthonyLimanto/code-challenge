export type DueCharges = {
    id: string,
    accountId: string,
    date: string, // mocked data the date is just a string, can easily change to accomendate a date object instead
    amount: number
}